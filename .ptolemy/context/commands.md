# Commands

- `make test` - run the project test suite if the Makefile target is available.
- `make build` - build the worker binary if the Makefile target is available.
- `/usr/local/go/bin/go test ./...` - run all Go tests in WSL.
- `/usr/local/go/bin/go run ./cmd/workerd` - start the worker daemon locally.
