# KB Workflows

- Read `.ptolemy/PTOLEMY.md` before broad repo inspection.
- Use `FILE_INDEX.json` and `SYMBOL_INDEX.json` to narrow candidate files before scanning.
- Rebuild the KB with the navigator build surface when bootstrapping a workspace.
- Update the KB once per successful task pack, after integration merge and before push or PR creation.
